<% [:notice, :success, :error, :alert].each do |key| %>
  <% unless flash[key].blank? %>
    <div class="<%= flash_class(key) %> fade in">
      <a href="#" data-dismiss="alert" class="close">×</a>
      <%= flash[key]%>
    </div>
  <% end %>
<% end %>
